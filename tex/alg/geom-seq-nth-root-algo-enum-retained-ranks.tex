% TODO: missing pseudocode
\begin{algorithm}
\caption{Geometric Sequence $n$th Root Stratum Enumeration}
\label{alg:geom-seq-nth-root-enum-retained-ranks}
\begin{algorithmic}
    \REQUIRE{ $\texttt{n}$ -- the number of strata deposited }
    \REQUIRE{ $\texttt{r}$ -- the interspersal (aka desired resolution) }
    \REQUIRE{ $\texttt{d}$ -- the degree } % TODO: what is this?
    \ENSURE{ set of retained strata }
\end{algorithmic}

\begin{algorithmic}[1]
    % TODO: add in algo
\end{algorithmic}
\end{algorithm}
